# 复制为 .env 后填入你的局域网 IP（手机上的 localhost 指向手机自己，必须填电脑的 IP）
# 查看本机 IP：macOS 用 ipconfig getifaddr en0，Linux 用 hostname -I
EXPO_PUBLIC_API_URL=http://192.168.1.100:8787

# 走 HTTPS 时（生产或已配自签证书）：
# EXPO_PUBLIC_API_URL=https://your-domain:8787
